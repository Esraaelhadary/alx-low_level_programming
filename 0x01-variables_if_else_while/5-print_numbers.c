#include <stdio.h>
/**
 * main - entry point
 * Description: prints all single digit numbers of base 10 starting from 0
 * Return: 0 (Success)
 */
int main(void)
{
char single = '10';
while (single >= 0)
{
putchar(single);
single++;
}
puthar('\n');
return (0);
